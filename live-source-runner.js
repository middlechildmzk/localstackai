(function(){
  const SOURCES = [
    { id:'github', label:'GitHub users', enabled:true, resultType:'candidate' },
    { id:'stackoverflow', label:'Stack Overflow users', enabled:true, resultType:'candidate' },
    { id:'huggingface', label:'Hugging Face authors', enabled:true, resultType:'candidate' },
    { id:'npm', label:'npm maintainers', enabled:true, resultType:'candidate' },
    { id:'hn', label:'HN stories/authors', enabled:false, resultType:'evidence' },
    { id:'openalex', label:'OpenAlex papers', enabled:false, resultType:'evidence' }
  ];
  const liveState = { results: [], loading:false, lastQuery:'', activeSources: SOURCES.filter(s=>s.enabled).map(s=>s.id) };
  const $ = id => document.getElementById(id);
  const esc = (s='') => String(s).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  const clean = (s='') => String(s || '').replace(/[\u2013\u2014]/g,'-').replace(/\s+/g,' ').trim();
  const words = (s='') => clean(s).split(/[,;\n\s]+/).map(x=>x.trim()).filter(x=>x.length>1);
  const compactQuery = (q, maxWords=10) => words(q).slice(0,maxWords).join(' ');
  const uniq = arr => [...new Map((arr || []).filter(Boolean).map(x => [String(x.url || x.id || x.title || Math.random()), x])).values()];
  const clearanceRe = /(TS\/SCI|Top Secret|Secret|Public Trust|polygraph|CI Poly|FSP|Full Scope|security clearance|active clearance)/i;

  function toast(msg){ const el=$('toast'); if(el){ el.textContent=msg; el.classList.add('show'); setTimeout(()=>el.classList.remove('show'),1600); } else console.log(msg); }
  function injectStyles(){
    if($('liveRunnerStyles')) return;
    const style=document.createElement('style'); style.id='liveRunnerStyles'; style.textContent=`
      .live-runner-card{border:1px solid var(--line,#29405f);border-radius:20px;background:linear-gradient(180deg,rgba(255,255,255,.055),rgba(255,255,255,.025));box-shadow:0 18px 60px rgba(0,0,0,.28);padding:15px;margin-bottom:16px}
      .live-runner-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;margin-bottom:12px}.live-runner-head h2{margin:0;font-size:18px}.live-runner-head p{margin:4px 0 0;color:var(--muted,#bfd0e8);font-size:13px}
      .live-source-toggles{display:flex;gap:8px;flex-wrap:wrap;margin:10px 0}.live-source-chip{border:1px solid var(--line,#29405f);border-radius:999px;background:rgba(3,9,20,.34);color:var(--muted,#bfd0e8);padding:7px 10px;font-size:12px;cursor:pointer}.live-source-chip.on{border-color:rgba(88,199,243,.42);background:rgba(88,199,243,.12);color:#d4f2ff}.live-source-chip input{margin-right:6px;width:auto}
      .live-result-list{display:grid;gap:10px;margin-top:12px}.live-result-card{border:1px solid var(--line,#29405f);border-radius:17px;background:rgba(3,9,20,.52);padding:13px}.live-result-top{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.live-result-title{font-weight:900;font-size:15px;color:var(--text,#f2f7ff)}.live-result-meta{font-size:12px;color:var(--faint,#8aa0bb);margin-top:3px}.live-result-snippet{font-size:13px;color:var(--muted,#bfd0e8);margin:9px 0;line-height:1.5}.live-badge{font-size:11px;border:1px solid rgba(88,199,243,.27);background:rgba(88,199,243,.09);color:#d4f2ff;border-radius:999px;padding:5px 8px;display:inline-flex;align-items:center;gap:4px}.live-badge.warn{border-color:rgba(255,209,102,.32);background:rgba(255,209,102,.09);color:#ffe6a8}.live-badge.good{border-color:rgba(82,210,115,.32);background:rgba(82,210,115,.09);color:#d5ffe0}.live-badge.grey{border-color:rgba(180,190,205,.25);background:rgba(180,190,205,.08);color:#d8e1ee}.live-card-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.live-small-btn{border:1px solid var(--line,#29405f);background:rgba(255,255,255,.055);color:var(--text,#f2f7ff);border-radius:11px;padding:8px 9px;font-size:12px;cursor:pointer;text-decoration:none}.live-small-btn.primary{background:rgba(88,199,243,.14);border-color:rgba(88,199,243,.35)}.live-empty{border:1px dashed var(--line,#29405f);border-radius:16px;padding:18px;color:var(--muted,#bfd0e8);text-align:center;background:rgba(3,9,20,.34)}.live-section-label{font-size:12px;text-transform:uppercase;letter-spacing:.08em;color:var(--blue,#58c7f3);font-weight:900;margin:16px 0 8px}`; document.head.appendChild(style);
  }
  function buildUi(){
    if($('liveSourceRunner')) return;
    const command=document.querySelector('.command-card'); if(!command || !command.parentNode) return;
    const card=document.createElement('section'); card.id='liveSourceRunner'; card.className='live-runner-card';
    card.innerHTML=`<div class="live-runner-head"><div><h2>Search Public Sources</h2><p>Find candidate-like public profiles first. Papers, packages, stories, and questions are labeled as evidence signals only. Use LinkedIn, ClearanceJobs, Indeed, and Avature manually.</p></div><div class="live-runner-actions"><button class="btn secondary" id="liveRunBtn">Search Public Sources</button><button class="btn ghost" id="liveClearBtn">Clear Results</button></div></div><div class="live-source-toggles" id="liveSourceToggles"></div><div id="liveResultStatus" class="subtle">Type a role, skill, company, or natural-language search above, then click Search Public Sources.</div><div id="liveResults" class="live-result-list"><div class="live-empty">No public-source results yet. Try: <strong>kubernetes terraform govcloud devsecops</strong>.</div></div>`;
    command.insertAdjacentElement('afterend', card); renderToggles(); $('liveRunBtn').addEventListener('click', runLiveSearch); $('liveClearBtn').addEventListener('click',()=>{ liveState.results=[]; renderResults(); setStatus('Results cleared.'); });
  }
  function renderToggles(){ const el=$('liveSourceToggles'); if(!el) return; el.innerHTML=SOURCES.map(s=>`<label class="live-source-chip ${liveState.activeSources.includes(s.id)?'on':''}"><input type="checkbox" data-live-source="${s.id}" ${liveState.activeSources.includes(s.id)?'checked':''}>${s.label}<span class="live-badge ${s.resultType==='candidate'?'good':'grey'}">${s.resultType==='candidate'?'lead':'evidence'}</span></label>`).join(''); el.querySelectorAll('input[data-live-source]').forEach(input=>input.addEventListener('change',e=>{ const id=e.target.dataset.liveSource; liveState.activeSources=e.target.checked ? [...new Set([...liveState.activeSources,id])] : liveState.activeSources.filter(x=>x!==id); renderToggles(); })); }
  function setStatus(msg){ const el=$('liveResultStatus'); if(el) el.textContent=msg; }
  function commandText(){ return clean(($('commandInput')&&$('commandInput').value)||($('roleTitle')&&$('roleTitle').value)||''); }
  function roleContextTerms(){ return clean(['roleTitle','mustHaves','niceHaves','targetCompanies','location','feedback'].map(id=>$(id)?.value||'').join(' ')); }
  function searchQuery(){ const cmd=commandText(); return cmd && cmd.length>2 ? cmd : roleContextTerms(); }

  async function runLiveSearch(){
    const q=searchQuery(); if(!q || q.length<2){ toast('Add a search first'); return; }
    liveState.loading=true; liveState.lastQuery=q; liveState.results=[]; renderResults(); setStatus('Searching public sources...');
    const active=liveState.activeSources; const jobs=[];
    if(active.includes('github')) jobs.push(searchGitHub(q));
    if(active.includes('stackoverflow')) jobs.push(searchStackOverflowUsers(q));
    if(active.includes('huggingface')) jobs.push(searchHuggingFaceAuthors(q));
    if(active.includes('npm')) jobs.push(searchNpmMaintainers(q));
    if(active.includes('hn')) jobs.push(searchHN(q));
    if(active.includes('openalex')) jobs.push(searchOpenAlex(q));
    const settled=await Promise.allSettled(jobs); const results=settled.flatMap(x=>x.status==='fulfilled'?x.value:[]);
    liveState.results=uniq(results).slice(0,60); liveState.loading=false; renderResults(); setStatus(`Found ${liveState.results.length} public-source results for: ${compactQuery(q,14)}`);
  }
  function caveatFor(r){ return clearanceRe.test(`${r.title} ${r.subtitle} ${r.snippet} ${(r.evidence||[]).join(' ')}`) ? 'Candidate-stated/public mention only. Not verified clearance.' : 'Public-source signal only. Review manually.'; }
  async function searchGitHub(q){ const query=encodeURIComponent(`${compactQuery(q,8)} in:login in:name in:bio`); const res=await fetch(`https://api.github.com/search/users?q=${query}&per_page=10`,{headers:{Accept:'application/vnd.github+json'}}); if(!res.ok) throw new Error('GitHub rate limit or API error'); const data=await res.json(); const hydrated=await Promise.all((data.items||[]).slice(0,8).map(async u=>{ try{ const r=await fetch(u.url,{headers:{Accept:'application/vnd.github+json'}}); const d=await r.json(); return { id:'gh_'+u.id, resultClass:'candidate', source:'GitHub', type:'Candidate lead - public developer profile', title:d.name || u.login, subtitle:u.login, url:u.html_url, snippet:[d.bio,d.company,d.location,d.blog].filter(Boolean).join(' · ') || 'Public GitHub profile. Review repos/activity manually.', evidence:[d.public_repos?`${d.public_repos} public repos`:'', d.followers?`${d.followers} followers`:'', d.location?`Location: ${d.location}`:''].filter(Boolean), confidence:'Public profile signal' }; }catch{return null;} })); return hydrated.filter(Boolean); }
  async function searchStackOverflowUsers(q){ const url=`https://api.stackexchange.com/2.3/users?order=desc&sort=reputation&inname=${encodeURIComponent(compactQuery(q,5))}&site=stackoverflow&pagesize=10`; const res=await fetch(url); if(!res.ok) throw new Error('StackExchange API error'); const data=await res.json(); return (data.items||[]).map(user=>({ id:'so_'+user.user_id, resultClass:'candidate', source:'Stack Overflow', type:'Candidate lead - technical contributor profile', title:user.display_name, subtitle:user.reputation?`Reputation: ${user.reputation.toLocaleString()}`:'Contributor profile', url:user.link, snippet:`Stack Overflow contributor. Review answer history manually for technical depth in ${compactQuery(q,3)}.`, evidence:[user.reputation?`${user.reputation.toLocaleString()} reputation`:'', user.badge_counts?.gold?`${user.badge_counts.gold} gold badges`:'', user.location?`Location: ${user.location}`:''].filter(Boolean), confidence:'Technical contributor signal' })); }
  async function searchHuggingFaceAuthors(q){ const res=await fetch(`https://huggingface.co/api/models?search=${encodeURIComponent(compactQuery(q,8))}&limit=10`); if(!res.ok) throw new Error('Hugging Face API error'); const data=await res.json(); return (Array.isArray(data)?data:[]).map(m=>({ id:'hf_'+m.id, resultClass:m.author?'candidate':'evidence', source:'Hugging Face', type:m.author?'Candidate lead - AI/ML model author':'Evidence signal - AI/ML model', title:m.author || m.id, subtitle:m.author?`Model: ${m.id}`:'Model result', url:m.author?`https://huggingface.co/${m.author}`:`https://huggingface.co/${m.id}`, snippet:m.pipeline_tag?`Pipeline: ${m.pipeline_tag}`:'Hugging Face model result. Review author/profile manually.', evidence:[m.downloads?`${m.downloads} downloads`:'', m.likes?`${m.likes} likes`:'', m.tags?.length?`Tags: ${m.tags.slice(0,5).join(', ')}`:''].filter(Boolean), confidence:'AI/ML public work signal' })); }
  async function searchNpmMaintainers(q){ const res=await fetch(`https://registry.npmjs.org/-/v1/search?text=${encodeURIComponent(compactQuery(q,8))}&size=10`); if(!res.ok) throw new Error('npm API error'); const data=await res.json(); return (data.objects||[]).map(o=>{ const username=o.package.publisher?.username || o.package.maintainers?.[0]?.username || o.package.name; return { id:'npm_'+o.package.name, resultClass:username===o.package.name?'evidence':'candidate', source:'npm', type:username===o.package.name?'Evidence signal - package':'Candidate lead - open-source maintainer', title:username, subtitle:`Maintains: ${o.package.name}`, url:username===o.package.name ? (o.package.links?.npm || `https://www.npmjs.com/package/${o.package.name}`) : `https://www.npmjs.com/~${username}`, snippet:o.package.description || 'npm package/maintainer result. Review maintainer and repo manually.', evidence:[o.package.version?`Version: ${o.package.version}`:'', o.score?.final?`Score: ${Math.round(o.score.final*100)}%`:'', o.package.keywords?.length?`Keywords: ${o.package.keywords.slice(0,5).join(', ')}`:''].filter(Boolean), confidence:'Open-source maintainer signal' }; }); }
  async function searchHN(q){ const res=await fetch(`https://hn.algolia.com/api/v1/search?query=${encodeURIComponent(compactQuery(q,8))}&tags=story&hitsPerPage=10`); if(!res.ok) throw new Error('HN API error'); const data=await res.json(); return (data.hits||[]).map(h=>({ id:'hn_'+h.objectID, resultClass:'evidence', source:'Hacker News', type:'Evidence signal - community story/discussion', title:h.title || h.story_title || 'HN result', subtitle:h.author?`by ${h.author}`:'', url:h.url || `https://news.ycombinator.com/item?id=${h.objectID}`, snippet:'HN story/discussion. Use as market or contributor signal only, not a candidate record.', evidence:[h.points?`${h.points} points`:'', h.num_comments?`${h.num_comments} comments`:'', h.author?`Author: ${h.author}`:''].filter(Boolean), confidence:'Community signal' })); }
  async function searchOpenAlex(q){ const res=await fetch(`https://api.openalex.org/works?search=${encodeURIComponent(compactQuery(q,10))}&per-page=10`); if(!res.ok) throw new Error('OpenAlex API error'); const data=await res.json(); return (data.results||[]).map(w=>{ const authors=(w.authorships||[]).slice(0,3).map(a=>a.author?.display_name).filter(Boolean); return { id:'oa_'+w.id, resultClass:'evidence', source:'OpenAlex', type:'Evidence signal - research/publication', title:w.title || 'OpenAlex work', subtitle:authors.length?authors.join(', '):'', url:w.doi?`https://doi.org/${w.doi.replace('https://doi.org/','')}`:w.id, snippet:'Research/publication result. Save as evidence only unless manually converted after review.', evidence:[w.publication_year?`Year: ${w.publication_year}`:'', w.cited_by_count?`${w.cited_by_count} citations`:'', authors.length?`Authors: ${authors.join(', ')}`:''].filter(Boolean), confidence:'Research evidence signal' }; }); }

  function renderResults(){ const el=$('liveResults'); if(!el) return; if(liveState.loading){ el.innerHTML='<div class="live-empty">Searching public sources...</div>'; return; } if(!liveState.results.length){ el.innerHTML='<div class="live-empty">No public-source results yet. Run a search to populate candidate leads and evidence signals.</div>'; return; } const candidates=liveState.results.filter(r=>r.resultClass==='candidate'); const evidence=liveState.results.filter(r=>r.resultClass!=='candidate'); el.innerHTML=`${renderGroup('Candidate Leads', candidates)}${renderGroup('Evidence Signals Only', evidence)}`; attachActions(); }
  function renderGroup(label, rows){ if(!rows.length) return ''; return `<div class="live-section-label">${esc(label)}</div>${rows.map((r)=>renderResultCard(r, liveState.results.indexOf(r))).join('')}`; }
  function renderResultCard(r,i){ const caveat=caveatFor(r); return `<article class="live-result-card"><div class="live-result-top"><div><div class="live-result-title">${esc(r.title)}</div><div class="live-result-meta">${esc(r.source)} · ${esc(r.type)} ${r.subtitle?'· '+esc(r.subtitle):''}</div></div><span class="live-badge ${r.resultClass==='candidate'?'good':'grey'}">${r.resultClass==='candidate'?'Candidate lead':'Evidence only'}</span></div><div class="live-result-snippet">${esc(r.snippet)}</div><div>${(r.evidence||[]).map(e=>`<span class="live-badge">${esc(e)}</span>`).join(' ')}${caveat.includes('not verified')?` <span class="live-badge warn">${esc(caveat)}</span>`:` <span class="live-badge grey">${esc(caveat)}</span>`}</div><div class="live-card-actions"><a class="live-small-btn primary" href="${esc(r.url)}" target="_blank" rel="noreferrer">Open Source</a><button class="live-small-btn" data-copy-result="${i}">Copy Summary</button>${r.resultClass==='candidate'?`<button class="live-small-btn" data-review-result="${i}">Review as Candidate</button>`:`<button class="live-small-btn" data-evidence-result="${i}">Save Evidence</button>`}</div></article>`; }
  function attachActions(){ document.querySelectorAll('[data-copy-result]').forEach(btn=>btn.addEventListener('click',e=>copy(summary(liveState.results[Number(e.target.dataset.copyResult)])))); document.querySelectorAll('[data-review-result]').forEach(btn=>btn.addEventListener('click',e=>{ const r=liveState.results[Number(e.target.dataset.reviewResult)]; const input=$('commandInput'); if(input) input.value=summary(r); if(typeof window.reviewProfile==='function') window.reviewProfile(); toast('Loaded into profile review'); })); document.querySelectorAll('[data-evidence-result]').forEach(btn=>btn.addEventListener('click',e=>{ const r=liveState.results[Number(e.target.dataset.evidenceResult)]; if(window.SourcingOS14_5?.state){ const st=window.SourcingOS14_5.state; st.evidenceItems.unshift({ id:'ev_'+Date.now(), projectId:st.activeProjectId || '', evidenceType:'public_source', evidenceText:summary(r), sourceName:r.source, sourceUrl:r.url, confidence:'low', caveat:'Evidence signal only. Review manually.', createdAt:new Date().toISOString() }); window.SourcingOS14_5.addProjectMemory?.('evidence_signal', `Saved ${r.source} evidence: ${r.title}`, 'live-source-runner'); toast('Evidence saved'); } else copy(summary(r)); })); }
  function summary(r){ return `${r.title}\nSource: ${r.source}\nType: ${r.type}\nClass: ${r.resultClass==='candidate'?'Candidate lead':'Evidence signal only'}\nURL: ${r.url}\nSummary: ${r.snippet}\nEvidence:\n- ${(r.evidence||[]).join('\n- ')}\n\nCaveat: ${caveatFor(r)}`; }
  function copy(text){ navigator.clipboard.writeText(text).then(()=>toast('Copied')).catch(()=>toast('Copy failed')); }
  function init(){ injectStyles(); buildUi(); }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',init); else init();
  window.runLiveSourceSearch=runLiveSearch;
})();
